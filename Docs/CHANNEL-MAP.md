# APMF Channel Map (exhaustive)

The core blueprint: for each directable AI-control facet, its source, the clean **APMF DENY gate**
(APMF's ONLY lever — suppress the losing source, no re-assert), **how the CLIENT drives the facet**
(the client's concern, via its own proven mechanisms — NEVER an APMF action), version-robustness, and
a **DOCUMENTED vs GAP** verdict. Derived 2026-09-02 from CommonLibSSE-NG RE headers, the CK wiki, and
existing mods (TDM, NFF, PapyrusUtil, Puppeteer).

**Governed by design.md §1a + INVARIANTS #0: APMF MODERATES (arbitrate + DENY + a narrow, sanctioned
bounded-promote); it NEVER manufactures or sustains an AI decision.** The "how the client drives it"
column below is documentation of the CLIENT's execution path for arbitration-only facets — it is NOT a
list of things APMF does for those rows. APMF calls no decision-generating engine function
(`StartCombat`, `CastSpellImmediate`, movement drive-feed, a `selectedSpells`/`EquipShout`-style
selection write) -- except where an INVARIANTS #0 amendment names one: (e) the marker cast and (g) row
21's one `StartCombat` per client declaration ((h), row 22, calls nothing: it denies that same function's entry). Legitimate APMF actions are: arbitrate the claim; DENY at the source (AV writes that
gate the input the AI reads, movement full-block, detection AVs, package yield, pausing an actor's own
in-progress dialogue); and, for a facet with no meaningful deny form and no AI decision to arbitrate
around, PROMOTE a single bounded one-shot client-requested action (#0c — weapon draw ch.4, stance
toggle ch.3, idle/anim ch.12). (Earlier revisions of this file framed a "PROMOTE" column as APMF
DRIVING every facet, including decision-selection ones; that executor framing licensed a channel that
called StartCombat and CTD'd — it is corrected here. ch.14 shout-power previously called `EquipShout`
directly, the same anti-pattern in miniature; it is now arbitration-only, mirroring ch.6/ch.8.)

`RE/…` = CommonLibSSE-NG headers. `// 0xNN` = in-header vfunc index (index-stable across runtimes);
struct-member offsets are accessor/Address-Library covered but more version-sensitive. Taxonomy is
OPEN (21 channels + sub-splits: 1-17 plus ch.19, ch.20, ch.21 and ch.22; ch.18 is RESERVED for an attack-selection design that
is NOT YET ON MAIN and has not been authored). Combat-target / combat-actions / casting (⭐) carry the weight.

## Table

| # | Channel | Source | APMF DENY gate (APMF's only lever) | How the CLIENT drives it (NOT an APMF action) | Verdict |
|---|---------|--------|-----------|---------|---------|
| 1 | **Movement / locomotion** | Package planner (`AIProcess.currentPackage`) → `MovementControllerNPC`→`ActorMover` | **FULL BLOCK (built):** `KeepOffsetFromActor(self, 0)` nulls the move GOAL at the source + `SetDontMove` locks translation → clean stand-still (no run-in-place, no teleport-snap; `SetDontMove` ALONE was one layer too shallow). Both Address-Library bound | `IMovementDirectControl` feed (unnamed `Unk_01..08`) or `Actor::Move` 0xC8 (probe-gated) | **DENY/full-block DOCUMENTED; PROMOTE feed GAP** |
| 1a | ↳ **Gait / speed** | `PreferredSpeed` walk/jog/run; `kSpeedMult` AV=30 | **TRUE:** set `kSpeedMult` AV | same AV / package speed flag | **DOCUMENTED, clean** |
| 2 | **Facing / heading** | Same planner (rotation) | rides movement gate (`SetAIDriven`) | same direct-control feed | shared w/ movement; standalone = GAP |
| 3 | **Stance / sneak** | Package flag → `actorState1.sneaking` | clean only via package flag; anim-event = additive | `NotifyAnimationGraph("SneakStart/Stop")` vfunc 01 | **DOCUMENTED (Tier A), bounded one-shot promote — #0c** |
| 4 | **Weapon draw / sheathe** | `actorState2.weaponState` (sticky) | sticky, not per-frame | `DrawWeaponMagicHands(bool)` vfunc 0xA6 | **DOCUMENTED, bounded one-shot promote — #0c** |
| 5 | **Headtracking** | AI writes per-type `HighProcessData.headTrackTarget[]` | **TRUE:** own the slot (`SetHeadtrackTarget(type,ref)`) | same setter | **DOCUMENTED, clean gate** |
| 6 | **Combat-target** ⭐ | `AIProcess.currentCombatTarget` (+ `CombatController.targetHandle`), re-chosen each tick by threat | ARBITRATION-ONLY today (record owner). FUTURE deny: suppress a competing framework's target write at the hook (GAP) | CLIENT commands it: compare-and-write of `currentCombatTarget` (MFO's `Targeting::Command` + its UpdateCombat hook re-assert; StartCombat to initiate) — APMF makes NO combat call | **APMF arbitration-only; client executes the command.** Unchanged by ABI v13: a client that wants APMF to HOLD the target claims row 20 `kIntent_TargetPin`, a separate intent |
| 7 | **Combat ACTIONS** ⭐ | Internal **combat behavior tree** (`CombatBehaviorController`) + `CombatInventory` + `CombatState` | **TRUE (graduated 2026-09-03; paired 2026-09-04):** `write_vfunc` slots 0x02 (act) **and 0x03 (pop)** on all 70 `CombatBehaviorTreeNodeObject_*` leaves (`core/ActionGate.cpp`, T1) — a `kIntent_CombatAction` claim denies exactly the leaves whose classified category bit is set in `APMF_Param.ival` (starts with `kCombatActionCat_Offense`); a denied leaf runs `CombatBehaviorForceFail`'s own original `act()`+`pop()` PAIR (thread-local pending-pop), never a hand-reconstructed `SetFailed` call and never act() alone (the unpaired form corrupted the thread's data stack — INVARIANTS #18) | `channels/CombatAction.cpp` claims the facet (arbitration-only, names the deny mask); real lever otherwise = loadout (ch.15) + aggression (ch.11) | **DOCUMENTED, clean gate (graduated from the field-proven T1 probe)** |
| 7a | ↳ attack/block/power/bash | `ATTACK_STATE_ENUM`; `meleeAttackState`; melee-vs-ranged = equipped weapon | (share ch.7 gate above; Attack/AttackLow/Bash leaves classified "offense") | — | **DOCUMENTED (ch.7)** |
| 8 | **Casting** ⭐ | SELECT: `Actor.selectedSpells[slot]`→`MagicCaster.currentSpell`. TRIGGER: internal combat-caster state machine | **ARBITRATE + DENY (gated)** — records the owner AND denies any spell/staff that is not the claim's `param.form` (plus its `SetSpellAllowList` set) at BOTH gates: `core/CastGate.cpp:124` (0x0A `CheckCast`) and `core/EquipGate.cpp` (0x0F `CheckShouldEquip`). (This cell read "ARBITRATION-ONLY today" until 2026-09-07 — stale since the deny landed 2026-09-02.) FUTURE deny: suppress a COMPETING framework's cast selection (GAP) — NOT applied on the owned-cast path (the client WANTS its AI to cast) | CLIENT makes a REAL animated cast: equip spell + write own `selectedSpells[slot]` + grant own AI consent (deny competing spells) + a Cast-biased combat style so the AI DECIDES to cast it — full animation, mobile, NO force, NO package. APMF makes NO cast write | **APMF arbitration-only; client executes the AI-decided cast** |
| 8+ACT | ↳ **Casting +ACT mode** — **RETIRED** (feat/ai-cast-seats-impl, 2026-09-05) | `ival` bit 2 (`kActFlag_Drive`) is now RESERVED and IGNORED. The bits stay in the byte-frozen ABI so a client that still sets them is byte-compatible; it simply gets row 8's gate-only behaviour. | — | The whole APMF-executed drive (`core/CastExecutor.cpp`: PhaseSelect/Rest/Drawn/Fire/Hold, ParkHand/TeardownHand, the wall-clock Budget plumbing, and the `CastSpellImmediate` guaranteed-delivery fallback that INVARIANTS #0 forbids by name) is DELETED. Superseded by ch.8b's engine seats, which are strictly better on every axis that mattered: real animation, real charge/fire/concentration, real magicka + LOS + interrupt handling, no equip race with the AI, and no manufactured effect application. The delivery-flip proxy pool SURVIVED (`core/CastProxy.{h,cpp}`) because the engine's `FindTargets` Self branch always lands on the caster. | **RETIRED — use ch.8b `kIntent_Cast`.** |
| 8a | ↳ L/R/dual/staff | `CastingSource` kLeftHand/kRightHand/kOther/kInstant; per-slot casters | (arbitration-only) | CLIENT owns the slot's spell via its own `selectedSpells` | **client per-hand** |
| **8b** | **Cast EXECUTION** ⭐ **THE KEYSTONE** | **SIX ENGINE SEATS** (feat/seat0-classify adds SEAT 0): `SEAT 0 CLASSIFY` (`core/CastClassify.cpp`, `VTABLE_CombatMagicItemData` slot 1 — forces a non-self heal/buff spell's effects to key into the SAME table row a self-heal uses, so the engine's classifier admits it at all; RE notebook J1-J9 found the OTHER five seats were correctly built but structurally idle because a heal-OTHER spell never got a `CombatInventoryItem` in the first place), `0x0F CheckShouldEquip` (`core/EquipGate.cpp`, WHICH item enters the hands — now also closes the "stale heal-other item heals the foe" gap, INVARIANTS #18), `0x06 CheckStartCast` (WHETHER), `0x0A GetMagicTarget` (WHERE — handle form), `0x07 CheckStopCast` (HOW LONG), `0x0D SetupAimController` (AIM, `+0x30` target override) — the last four in `core/CastSeats.cpp`, on the **`CombatMagicCasterRestore` AND `CombatMagicCasterOffensive` vtables ONLY** — 2 of 14, `CastSeats.cpp:483-500` (0x0A's impl is the SHARED base: an unscoped redirect would aim Stagger/Disarm/Reanimate effects at the ally. Offensive was added in v0.9.2: a claimed HOSTILE spell classifies into the Offensive caster, never Restore, so a claim on it was structurally inert. Safe because every thunk re-tests {claim actor, claim's driven form} per call). Plus the pre-existing exclusivity denies (`CastGate` 0x0A CheckCast, `EquipGate` 0x0F, both per-hand) and the bounded TTL auto-release. | **ARBITRATE + DENY + COMPOSE.** While the claim stands, the NPC's OWN AI classifies, selects, equips, charges, aims, fires and channels the claimed spell at the claimed target — engine animation, engine magicka, engine LOS/interrupts. Two answers in this codebase do NOT chain: `SEAT 0` mutates an input field the classifier itself reads BEFORE calling it (composition, not a bypass); `0x0F` (the Restore templates' `CheckShouldEquip` IS the vanilla self/foe health gate `0x81f7c0`, which reads its target straight off the `CombatController` — there is no interposable seat, so chaining cannot express "heal the ally"). See INVARIANTS #20. SEAT 0 is AE-only (SE/VR refused — the RE notebook could not verify the classifier resolver's struct layout off 1.6.1170). | **APMF FIRES NOTHING** — no `EquipSpell`, no `CastSpell`/`CastSpellImmediate`, no `StartCharge`/`InterruptCast`, no `NotifyAnimationGraph`, no `selectedSpells`/`desiredTarget` write. A **kSelf** spell still needs the delivery-flip proxy (`core/CastProxy.h`): `FindTargets`' Self branch (0x5bc98a) resolves to the caster's own ref and never reads `desiredTarget`. The proxy is minted + transiently taught on the WRITER thread at claim-apply and freed one main-thread hop AFTER the cleared claim publishes. `core/EquipGate.cpp` also now sets `CombatInventory::dirty` on every call while a claim stands, so the classifier re-runs on the next tick without depending solely on MFO's own combat-style-swap side effect. | **BUILT and FIELD-RUN.** Deck-proven 2026-09-05 (v0.9.1, `STATUS.md`) and again on 2026-09-06 (17 animated offense fires plus the heal path). This cell said "NOT yet field-run" until 2026-09-07. What the 2026-09-06 session ALSO showed is that the claim is only half of an actor: the unclaimed hand stays AI-governed and the claim expires at its TTL — `Docs/DENY-COMPLETENESS-AUDIT.md` open gaps 9-13. |
| 9 | **Package-procedure activity** | `TESPackage.procedureType` (sandbox/patrol/guard) | **TRUE (graduated 2026-09-03):** `write_vfunc` on `VTABLE_Character[0]` slot 0x49 `CheckForCurrentAliasPackage` (`core/PackageGate.cpp`, T3) — a `kIntent_OfferPackage` claim returns the `TESPackage` named by `APMF_Param.form` instead of the framework's own answer; never-null fallback to the engine's answer on an unresolvable FormID | `channels/OfferPackage.cpp` claims the facet (arbitration + the `EvaluatePackage` nudge, names the package); CLIENT owns the package's own runtime target (e.g. a targType-0 handle) | **PROVEN WHEN NUDGED, and only then (corrected 2026-09-07).** The hook redirects whenever the engine ASKS while a published claim stands, and every field win so far reconciles to an explicit `EvaluatePackage` nudge: 16/16 deck wins followed a nudge, and the engine's own evaluation cadence contributed ZERO natural hits over ~75 s (MFO `Docs/DIAG-2026-09-06-loot-travel.md`). The mechanism is real and it is NOT self-sustaining. The graduated CHANNEL's own first field run engaged **0 of 6** dispatches — the "field-proven for engage/release" credit belonged to the retired PROBE, not to ch.9 as shipped; the channel's nudge fired BEFORE the claim published, so the 0x49 answer the engine got was the pre-claim one. Fixed and MERGED to `main` 2026-09-07 (`fix/apmf-offerpackage-nudge-ordering`): the nudge is now `mainthread::Post`ed, so it runs one hop PAST the claim's `Publish()`. Untagged, and NOT yet re-run on a deck — 0-of-6 has not yet been shown to be 6-of-6. |
| 10 | **Dialogue / greeting** | AI greeting/dialogue + topics | `SetDialogueWithPlayer` 0x41 / `StopCurrentDialogue` 0x4F | `InitiateDialogue` 0xD8 | **DOCUMENTED** (coarse) |
| 11 | **AI-attribute** (aggression / confidence / assistance / morality) | Dynamic AVs the engine's own combat/flee/assist decisions read (`kAggression`/`kConfidence`/`kMorality`/`kAssistance`) | **TRUE — the design's preferred model:** `ActorValueOwner::SetActorValue`/`ModActorValue` sets the input the AI itself consumes; no override | same setter | **DOCUMENTED, cleanest gate** |
| 12 | **Idle / animation** | AI idle manager | additive (no gate on AI's own idles) | `AIProcess::PlayIdle` one-shot; `NotifyAnimationGraph` | **DOCUMENTED, bounded one-shot promote — #0c** |
| 13 | **Facial expression / mood** | FaceGen emotion (`EmotionType`) | `ClearExpressionOverride` releases | **SetExpressionOverride not exposed** in this CommonLib build (raw fn needed) | **GAP** (setter) |
| 14 | **Shouts / abilities / powers** | `selectedPower`; voice slot; `GetCurrentShout` | ARBITRATION-ONLY (record voice-slot owner). FUTURE deny: suppress a competing framework's own `EquipShout` write (GAP) | CLIENT selects it: its own `ActorEquipManager::EquipShout` (sticky select; AI still triggers it) — APMF makes NO equip write | **APMF arbitration-only; client executes the select — mirrors ch.8** |
| 15 | **Equipment** (equip/unequip specific item) | Combat/package equip choices — sets what ch.4/7/8 work with | own the equipped set (remove an item = deny the AI that option — clean input-gate) | `ActorEquipManager::EquipObject`/`UnequipObject`/`EquipSpell`/`EquipShout` (scar: off-main → `MainThread::Post`, MFO #62) | **DOCUMENTED — the melee-vs-ranged lever.** GATE-ONLY facet (2026-09-03, `param.form` set): T2a's `EquipGate.cpp` `CheckShouldEquip` hook (ch.8's SAME hook) now also reads a `kIntent_Equipment` claim and denies any spell/staff re-arm while it stands (the AI's weapon-order gate, mirrors ch.8's arbitration-only shape — `channels/Equipment.cpp` makes no engine write for a param'd claim). The no-param hotkey probe (active unequip/re-equip) is unchanged. |
| 16 | **Detection / stealth** | `sneaking` (=ch.3); `HighProcessData.detectionModifier`; AVs kDetectLifeRange/kMovementNoiseMult | **TRUE:** set the detect AVs / `detectionModifier` | same | **DOCUMENTED** (AV path clean) |
| **19** | **TRAVEL** (`kIntent_Travel`, ABI v10, 2026-09-22) | Nothing in vanilla MOVES an NPC to a place on demand. `StartCombat` on an undetected foe produces the engine's SEARCH behaviour, not an approach (MFO ENGINE_NOTES 0.12: a foe at 2792u never entered combat), and there is no "walk here" call at all -- movement is a PACKAGE, so the destination has to be a real AI package | **ADDS NO SEAT, AND CLAIMS NO OTHER INTENT.** ch.19 files exactly ONE internal claim: a `kIntent_OfferPackage` (ch.9) naming an **APMF-owned** Travel package from `Data/APMF.esl`, whose `Place to Travel` Location input `core/PackageData.cpp` points at the destination ref (PLDT type 0, kNearReference) together with this leg's radius. That offer is the IMPLEMENTATION of travel, not a composed client intent, and it is filed -- and RE-FILED on an owner change -- at the CLIENT's own basis, so a competing client still wins a facet it outbids for. Delivery is ch.9's already-proven 0x49 redirect + posted `EvaluatePackage` nudge | `channels/Travel.cpp` claims the facet with `param.form` = the destination -- an object REFERENCE or a CELL, decided by the FormID's own record type, no flag -- `fval` = arrival radius (0 => 75u, clamped 50-512; not consulted for a cell, whose arrival test is parent-cell identity), `ival` = `TravelFlags`. **WHICH LOCATION KINDS ch.19 CAN HONESTLY WRITE, and the evidence for each** (`PackageLocation::AllocateLocation`, vtable slot 1, decoded on BOTH unpacked images 2026-09-22 -- SE fn `0x441BE0` / jump table `0x442194`, AE fn `0x49C9E0` / table `0x49CF94`; every case body is instruction-for-instruction identical across the runtimes): **0 `kNearReference` SUPPORTED** -- case reads `mov eax, DWORD [rsi+0x10]`, a 4-byte handle, which is what MFO field-proved. **1 `kInCell` SUPPORTED** -- case reads `mov rcx, QWORD [rsi+0x10]`, an 8-byte POINTER, null-checks it and uses it as `this`; so the write is `data.object = the CELL`, not a handle. **2 `kNearPackageStartLocation` / 3 `kNearEditorLocation` / 12 `kNearSelf` REFUSED** -- their cases read the CONTEXT or `.rdata` constants and never the struct's payload (all 154 vanilla Travel instances of these three carry payload 0), so there is nothing a client could name. **4 `kObjectID` / 5 `kObjectType` / 7 `kAtPackagelocation` REFUSED WITH PROOF** -- the jump table sends all three STRAIGHT TO THE EPILOGUE; the engine does not implement them at this vfunc at all, and zero vanilla Travel instances use them. **6 `kNearLinkedReference` REFUSED** -- writable (an 8-byte KEYWORD pointer) but it resolves against the ACTOR's own linked ref, so the client would not be naming a destination and APMF would be choosing one. **8/9 `kAlias_*` REFUSED** -- a 4-byte ALIAS INDEX resolved against the OWNING QUEST's alias machinery; our record carries no QNAM and giving it one would hijack that quest. **A WORLD POSITION IS NOT EXPRESSIBLE AT ALL -- a hard NOT FOUND, proven three ways:** `PackageLocation` is 0x18 bytes (vptr, locType, rad, an 8-byte union of `TESForm*`/`ObjectRefHandle`) with no coordinate storage; the on-disk PLDT is 12 bytes in ALL 1988 vanilla Travel-template instances in Skyrim.esm; and no switch case reads coordinates out of the struct. Vanilla's idiom is an XMarker REFERENCE, i.e. case 0, so a client that wants a point passes a marker ref -- and a claim carrying `param.pos` is REFUSED, never reinterpreted. The client does NOTHING else: no tick, no package to ship, no `StartCombat`, no `currentCombatTarget` write, no detection poke. `Repoint` moves the destination in place | **BUILT, OBSERVE-ONLY, not yet field-run.** The whole contract, in marth's words (2026-09-22): *"it goes to the target 50-100u from it. And combat interrupts and cancels the movement. That's all."* A leg ends on ARRIVAL, on the ACTOR ENTERING COMBAT (`Actor::IsInCombat`, Character vtable 0xE3 -- verified on both unpacked images as a null-check of `combatController` plus one byte test, so an actor with no controller reads false cleanly), or on the destination being gone (deleted, disabled, or an actor that was ALIVE when targeted dying during travel -- a destination already dead at target time is a corpse to walk to and never ends the leg by being dead, v0.9.7). **There is NO line-of-sight and NO detection test**: an earlier cut had both, and ending on LOS was actively wrong -- LOS is geometry, not knowledge, and it is already true at engage time in the send-them-at-a-visible-foe case, so the leg ended on the first poll before the actor moved (Fable tier-3 on 2d6108f, SEV-3). **THE DESTINATION NEED NOT BE LOADED OR NEARBY**: an actor paths across cells to a ref it cannot see, which is what a vanilla Travel package is for. An earlier cut ended a leg the instant the destination's 3D went unloaded, forbidding that by construction and ending every distant leg on the FIRST poll before the actor moved (Fable round 2 on 3c8adbf, SEV-3); the test is GONE, and the arrival compare never needed it (`GetPosition()` reads `data.location`, valid with no 3D attached). A hopeless leg is bounded by the combat cancel, the client's Release, and the 120 s STUCK net -- not by a reachability guess, and deliberately not by a cross-worldspace refusal (an interior has no worldspace, so that test would repeat the same mistake). **`INVARIANTS #0` is UNTOUCHED**: travel calls no decision-generating engine function. It claims nothing else on the client's behalf -- marth, *"We should avoid combining intents anyway"*; convenience COMBO intents are deferred until after the full MFO port, not refused. |
| **20** | **TARGET PIN** (`kIntent_TargetPin`, ABI v13, 2026-09-25) | `CombatController.targetHandle` + `AIProcess.currentCombatTarget`, both written by `CombatController::SetTarget` (1.6.1170 `0x559630` / 1.5.97 `0x4FDFE0`) from `CombatController::UpdateTarget` (`0x559930` / `0x4FE300`, called once per `Actor::UpdateCombat`), which takes the first non-zero answer of the active `CombatTargetSelector`s (vtable slot 6 `SelectTarget`, Standard `0x84CFE0` / `0x7B5E50`, Fixed `0x84DAF0` / `0x7B6920`). Runs on BSJobs workers, several actors in parallel (MFO ENGINE_NOTES 0.47) | **SOURCE DENY of the engine's pick** (`channels/TargetPin.cpp`): write_vfunc on slot 6 of BOTH selector vtables, chaining; for the actor's WINNING claim the answer is replaced with the pinned handle when the engine's own answer is non-zero AND the pin is in `combatGroup->targets` (read under the group lock) and not flagged `kTargetLost`. The engine's pick never reaches `SetTarget`. The pin ENDS -- APMF releases the claim itself (`targetpin::Poll`, logged with the reason) -- when the target is lost, dead, disabled, unloaded or unresolvable, or the owner dies; it only pauses while the target is not yet a combat target. The engine CLEARING the target is not denied. `UpdateCombat` (Character slot 0xE4) is hooked OBSERVE-ONLY to log a source-seat MISS or an OVERWRITE. A competing framework's own target write after the update is NOT denied (row 6 gap) | The CLIENT declares {actor, target}: `RequestEx(actor, kIntent_TargetPin, basis, &{form = target})`; `Repoint` moves the pin; `Release` ends it. Getting the actor into combat, and making the target a combat target of its group, is not this intent | **BUILT, CI-only, not yet field-run, and the selector seat has not yet been OBSERVED running (principle 5).** The first field log must show `FIRST SOURCE DENY` and no `SOURCE SEAT MISSED`. Refused synchronously on VR, a runtime other than exactly 1.6.1170 / 1.5.97, `[TargetPin] bTargetPin=0`, a self-check refusal of any of the three vtables, form 0, a self-target, or the player; a non-actor form is refused at Engage (live, inert handle). `INVARIANTS #0 (f)` licenses it |
| **21** | **COMBAT ENTRY** (`kIntent_CombatEntry`, ABI v14, 2026-09-25) | The actor's combat STATE: `Actor::StartCombat` (1.6.1170 id 38561 `0x6B6930` / 1.5.97 id 37608 `0x6251B0`; `bool(Actor* this, Actor* target, CombatGroup* join)`, verified register by register on both images) builds the `CombatController` (AE +0x160 / SE +0x158) and a new `CombatGroup` and makes the target the group's first combat target via `CombatGroup::AddTarget` (AE 44704 / SE 43482) -- if AddTarget fails the new controller is destroyed and the call returns false; already in combat it only adds the target to the group's targets (AE 33228 / SE 32481, result ignored, returns true). Ended by `StopCombat` (Character vtable 0xE5, AE `0x6B70A0` / SE `0x625920`) or the engine's own group / AI-reset paths | **NO DENY, NO SEAT.** ch.21 is INVARIANTS #0 (g): ONE engine call per client declaration. Nothing competing is denied (the engine's own target choice is row 20's facet; anyone may end the fight), and nothing is switched on as a substrate: combat is what the client declared, so its consequences (draw, StartCombat's own re-arm equip -- governed by ch.17 when claimed -- music, detection, crime, allies joining) are the client's | `channels/CombatEntry.cpp`: `param.form` = the target actor. One frame after the claim is published (re-validated), `actor->StartCombat(target, nullptr)` ONCE on the main thread; one more per Repoint / owner change. No Tick, no re-entry, no `StopCombat` on release. NEVER left live and inert: the claim ENDS (Harbinger releases it, reason logged) on an engine refusal, an entry that cannot be attempted (target not an Actor, actor unloaded / no process / dead), the fight ending (`combatController` pointer null after a successful entry: "combat ended", pointer read only), owner dead, or target dead / disabled / unloaded / unresolvable. A retry is a NEW request. Pair with row 20 (same target) to make the actor fight THAT target: entry puts it in the group, the pin answers the selector | **BUILT, NOT FIELD-RUN.** Exact 1.6.1170 / 1.5.97, VR refused, `[CombatEntry] bCombatEntry=1`, `SeatVerified` on StartCombat (row `CombatEntry.Actor.StartCombat`). Sync refusal: channel unavailable, form 0, self, player as the actor. Passive rate-limited `[ch.21]` log: requested / entered or refused / target is a combat-group target (new fight: yes by construction, it is the true return; already in combat: not read, no group dereference) / whether a ch.20 claim names the target |
| **22** | **COMBAT RE-ENTRY DENY** (`kIntent_CombatReentryDeny`, ABI v15, 2026-09-25) | The actor's combat START: every non-load `CombatController` is built inside `Actor::StartCombat` (AE id 38561 `0x6B6930` / SE id 37608 `0x6251B0`) through CombatManager AE 46873 / 46874 (SE 45573 / 45574), whose only callers are StartCombat, which also adds targets to a running fight. StartCombat is non-virtual with ten callers per runtime; its FOURTH refusal check is the virtual `IsDead(true)` on `this` (Character slot 0x99, AE `0x674ED0` / SE `0x5E3160`), called at AE `0x6B69C4` (return `0x6B69CA`) / SE `0x625242` (return `0x625248`), before its global spinlock and every side effect | **SOURCE DENY at the entry function's own self-check** (`channels/CombatReentryDeny.cpp`): write_vfunc on Character slot 0x99, chaining; the answer is changed from "alive" to "dead" only when the thunk's return address is that one site, the actor holds the WINNING claim, its window runs (from that claim's own request / last Repoint, set by `Settle` after Publish), and the call is not ch.21's own entry (`ClientEntryScope`). EVERY StartCombat for the actor is refused in the window (new entry and in-combat target-add; closing-round option (a)); no actor state is read. Every other IsDead caller gets the engine's answer | The CLIENT declares {actor, window}: `RequestEx(actor, kIntent_CombatReentryDeny, basis, &{fval = seconds})` (0 = 10 s, clamped to 120 s). The recipe for a retreat is: claim, then `Actor::StopCombat` ONCE on the first tick `IsClaimLive(handle)` reads true (the claim is PENDING until the next Drain publishes it; Harbinger stops nothing). `Repoint` restarts that claim's window; `Release` ends it; Harbinger ends it itself on "window elapsed" or "owner dead" (`reentrydeny::Poll`, or `Settle` for a takeover with no time left) | **BUILT, CI-only, not yet field-run, and the seat has not yet been OBSERVED running (principle 5).** The first field log must show `[ch.22] seat OBSERVED` and, for a claimed retreat, `FIRST DENY` and no `DENY MISSED`. Refused synchronously on VR, a runtime other than exactly 1.6.1170 / 1.5.97, `[CombatReentryDeny] bCombatReentryDeny=0`, a self-check refusal of the Character vtable or the call-site row, a negative / NaN window, or the player. `INVARIANTS #0 (h)` licenses it |

## Synthesis

**APMF's legit DENY/suppress gates (APMF's own actions — set the input the AI reads, no re-assert):**
- **AI-attribute AVs (ch.11)** — bias the engine's own combat/flee decisions; cleanest gate on the board.
- **Movement DENY / full-block (ch.1)**; **Gait/speed (ch.1a)** `kSpeedMult`; **Detection AVs (ch.16)**;
  **Equipment (ch.15)** (remove an item = deny the AI that option). These SUPPRESS; they do not drive.

**Behavior the CLIENT executes (APMF only arbitrates the facet, never does these):** combat-target
command (ch.6, client's `currentCombatTarget` write), casting (ch.8, client's `selectedSpells` + consent
+ Cast-style so the AI decides), and shout/power select (ch.14, client's own `EquipShout` — converted to
arbitration-only, mirroring ch.6/ch.8).

**APMF's sanctioned bounded one-shot promotes (#0c — no deny form, no AI decision to arbitrate
around, a single deterministic call at Engage/Release, no `Tick`, no re-assert):** weapon draw (ch.4,
`DrawWeaponMagicHands`), stance toggle (ch.3, `NotifyAnimationGraph SneakStart/Stop`), idle/anim
(ch.12, `NotifyAnimationGraph IdleForceDefaultState`). These are formally permitted, not pending
conversion — see INVARIANTS #0(c).

**Need live probing:** Movement PROMOTE feed (`IMovementDirectControl::Unk_0N`, §9.1 — biggest unknown);
the DENY of a competing framework's combat-target/cast/shout selection at the hook (ch.6/ch.8/ch.14 —
the future suppression gate); Facial-expression SETTER (ch.13). Combat ACTIONS (ch.7) and package
procedures (ch.9) are now gated — see rows above; ch.7's category classification currently covers only
"offense" and ch.9's field pass covered Phases 1-2 only (save/load, Phase 3, unexercised). **The
Cicero/travel "starve an outranking framework's package" case is what ch.9 was graduated to solve:**
`core/PackageGate.cpp`'s 0x49 hook returns the claimed client's package for a claimed actor regardless
of what any other framework's own package logic would otherwise offer — see design.md §1a.

## Combat / casting verdict (the MFO headline)
- **THE ENGINE MAKES THE CAST; APMF ONLY ANSWERS (feat/ai-cast-seats-impl, 2026-09-05).** The keystone
  changed shape. It used to be "the client fires its own animated cast and APMF keeps the AI out of the
  way." It is now: a `kIntent_Cast` claim {actor A, spell S, target T} is answered into the five vfunc
  seats the AI's own cast decision is built out of (row 8b), and the AI itself equips, charges, aims,
  fires and channels S at T. Nobody force-casts anything — not APMF, not the client. This is the honest
  end state of the "composition, not substitution" imperative: APMF substitutes the INPUTS the engine's
  own decision reads, never the decision, and never the behavior.
  - The one seat that cannot be answered by substituting an input (`0x0F CheckShouldEquip`, whose Restore
    override reads the `CombatController` directly) is answered from the claim WITHOUT chaining. That is
    a real, deliberate exception to "engine-answer-first"; INVARIANTS #20 states it and bounds it.
  - `kIntent_Cast` no longer denies the AI's cast leaves or its magic context node. It cannot: those ARE
    the delivery mechanism now. A client that wants "no casting at all" claims ch.7
    `kIntent_CombatAction` with `kCombatActionCat_Cast` — a separate, still-live intent.
- **The client makes the behavior; APMF arbitrates the facet.** MFO steers the target with its own
  `currentCombatTarget` write (`Targeting`), makes its follower's own AI DECIDE to cast the chosen spell
  via `selectedSpells` + `CasterConsent` (permit wanted, deny competing) + a Cast-biased combat style,
  and biases combat/flee via aggression/confidence AVs. The cast is REAL and fully ANIMATED (the vanilla
  AI path) and stays MOBILE — no `CastSpellImmediate` force, no rooting UseMagic package. APMF only claims
  the cast + combat-target facets (arbitration), leaving movement untouched. APMF calls neither
  `StartCombat` nor `CastSpellImmediate` (INVARIANTS #0).
- **Combat ACTIONS (attack/block/power/bash, melee micro) are now GATED (ch.7, graduated 2026-09-03)** — a
  `kIntent_CombatAction` claim denies the "offense" category (Attack/AttackLow/Bash/RangedAttack/
  SpecialAttack/GroundAttack/FlyingAttack/CastImmediateSpell/CastConcentrationSpell/CastShout/
  PrepareDualCast leaves) for its actor; every other leaf (movement, block, dodge, cover, search, ...)
  still fires natively. They can ALSO be SHAPED, as before: **equipment (ch.15)** dictates melee-vs-ranged
  and the available action set, and **aggression/confidence AVs (ch.11)** bias the tree's choices.
- **Net:** APMF lets MFO gambits fully PERFORM the target / spell / disposition layer of combat, DENY the
  offense category of the melee layer outright, and SHAPE what remains (loadout + disposition) — only a
  finer-grained melee-action category split (defense/movement/utility) is still future work.
