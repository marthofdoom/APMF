# Build output
build/
native/build/
native/out/
*.dll
*.pdb
*.lib
*.exp
# top-level out/ is NOT ignored: APMF_GenerateESP.py writes the generated
# APMF.esl + SEQ/APMF.seq there, and (mirroring MFO's own repo convention)
# those generated artifacts are committed, not rebuilt at CI time.

# vcpkg
vcpkg_installed/

# IDE / tooling
.vs/
.vscode/
*.user
CMakeUserPresets.json
compile_commands.json

# OS
.DS_Store
Thumbs.db
